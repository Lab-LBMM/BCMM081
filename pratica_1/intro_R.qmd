---
title: "BCMM081"
subtitle: "Prática 1 - Introdução à programação com R"
author: "Renato Augusto Corrêa dos Santos"
institute: "IB/UNESP Rio Claro"
date: today
date-format: "D [de] MMMM [de] YYYY"
lang: pt-BR
format:
  revealjs:
    theme: [default, intro_R.scss]
    slide-number: c/t
    show-slide-number: all
    footer: "Prática 1 - Introdução à programação com R"
    logo: "logo_lbmm_racs_hq.png"
    incremental: false
    code-copy: true
    code-link: true
    highlight-style: github
    chalkboard: true      # tecla B abre a lousa; C desenha sobre o slide
    menu:
      side: left
    preview-links: auto
execute:
  echo: true
  warning: false
  message: false
knitr:
  opts_chunk:
    comment: "#>"
    collapse: true
---

## Objetivos de hoje {.smaller}

[Roteiro]{.eyebrow}

::: {.incremental}
- Usar R como calculadora (operações matemáticas básicas)
- Objetos e atribuição: tipos de dados e variáveis
- Vetores, matrizes, arrays
- Importação/exportação de arquivos tabulares
- Objeto `data.frame` e operações com dataframes
- Primeiro gráfico
- Pacotes em R, com exemplos do `Tidyverse`
- Geração de gráficos com `ggplot2`
- Usar ferramentas de IA para auxiliar na programação em R
:::

::: {.notes}
Notas do apresentador ficam aqui e aparecem só na sua tela (tecla S).
Lembrar: pedir para abrirem o RStudio antes de começar.
:::

## Criando um arquivo R Markdown {.smaller}

::: {.incremental}
- File → New File → R Markdown
- Inclua título, autor e data
- Escolha o tipo de saída: `HTML`, `PDF` ou `Word`
:::

## Criando um script R {.smaller}

::: {.incremental}
- Arquivo (*File*)
- Novo arquivo (*New file*)
- R script
- Inclua códigos de R
- Execute o código
- Faça o *debugging*
:::


## Programação orientada a objetos {.smaller}

Tipos de dados

::: {.incremental}
 - Strings (`"Carolina"`)
 - Números inteiros (`12`)
 - *Floats* (`12.421`)
 - Números complexos (`0+12.43i`)
 - Valores lógicos (`TRUE` e `FALSE`)
 - Fatores (limitam o número de classes; geralmente definem códigos de grupos em estatística)

:::

## Programação orientada a objetos {.smaller}

Objetos

::: {.incremental}
 - Planilhas do Excel
 - Células, colunas, abas
 - Sequências de DNA, RNA, proteínas
 - Matrizes com dados de comunidades
 - ... ou séries temporais
 - ... ou saídas de testes estatísticos

:::

## Programação orientada a objetos {.smaller}

Variáveis

::: {.incremental}
 - São objetos que podem ter seus valores alterados
 - Usamos `=` ou `<-` para atribuir ou alterar valores associados a variáveis em R

:::

## Nomes que o R aceita {.smaller}

::: {.columns}
::: {.column width="50%"}
**Funciona**

```{r}
#| eval: false
abundancia
n_ninhos_2025
dados.brutos
```
:::
::: {.column width="50%"}
**Não funciona**

```{r}
#| eval: false
2ninhos      # começa com número
n ninhos     # tem espaço
TRUE         # nome reservado
```
:::
:::

R diferencia maiúsculas de minúsculas: `Dados` e `dados` são [dois objetos distintos]{.term}.

# Vetores e matrizes

## Propriedades de vetores {.smaller}

::: {.incremental}
 - Unidimensional (1D)
 - Células com endereço (índice) de 0 a *N*
 - Comprimento definido e pode ser recuperado com `length()`
 - Itens devem ser do mesmo tipo
 - Subconjuntos de vetores são acessados com `[]`

:::

# Lists

Atenção: Objetos do tipo `list` podem armazenar dados de tipos diferentes sem convertê-los.

## Propriedades de matrizes {.smaller}

::: {.incremental}
 - Bidimensional (2D)
 - Endereço com índices
   - Linhas têm índices antes de vírgula
   - Colunas têm índices depois de vírgula
 - Subconjuntos de vetores são acessados com `[linha, coluna]`

:::

## Arrays

Termo usado para qualquer objetivo com *N* dimensões

::: {.incremental}
 - Célula única (0)
 - Vetor (1D)
 - Matriz (2D)
 - Arrays com mais de 2 dimensões

:::

# Trabalhando com arquivos

Nossos dados científicos, especialmente de ômicas, geralmente estão representados em arquivos grandes armazenados em nossos computadores.


## Arquivo comumente usados

::: {.incremental}
 - Microsoft Excel (`.xlsx`)
 - Arquivos de texto facilmente exportados do Excel
   - Comma Separated Values (`.csv`)
   - Tab Separated Values (`.tsv`)
 - Importamos no R com a função `read.csv()`

:::

## Onde estamos ?

Funções básicas para se localizar no R:

::: {.incremental}
 - `getwd()`: verificar o caminho até o local de trabalho
 - `setwd()`: alterar o caminho até o local de trabalho

:::

## Objeto tipo data.frame

Como representamos dados tabulares no R?

::: {.incremental}
 - Tem duas dimensões, como uma matriz
 - Possui informações adicionais de indexação de linhas e colunas
 - Existem várias funções para trabalhar com `data.frame`

:::

## Dados de sequenciamento

::: {.incremental}
 - Dados de um experimento fictício
   - Arquivo `.csv` criado manualmente
 - RNA-Seq de leveduras (*Saccharomyces cerevisiae*)
   - Sequenciamento Illumina HiSeq 2500
   - Paired-end
   - Seleção por cDNA
   - Arquivo `.csv` baixado do SRA (NCBI)

:::

## Funções para recuperar subconjunto

::: {.incremental}
 - Sintaxe com `[]` e índices ou nomes de colunas
 - `subset()`

:::

## Operações para cálculo de estatísticas de grupos

::: {.incremental}
 - `aggregate()`
 - `tapply()` - gera um array
 - `lapply()` - `list`
 - `sapply()` - `vector` (vetor 1D)

:::

## Salvando arquivos no computador

::: {.incremental}
 - `write.csv()`

:::

## Controle de fluxo e estruturas condicionais

::: {.incremental}
 - Operador que avalia o valor `FALSE`/`TRUE`: `if() {}`
 - Operação para estabelecer uma alternativa, caso `FALSE`: `if() {} else() {}`
 - `if` aninhado: `if() {} else { if() {} else() {} }`
 - Versão compacta do if-else: `ifelse()`
 - Realiza uma operação até que certa condição seja estabelecida: `for() {}`
 - Laço aninhado: `for() { for() {} }`
 - Laço que se repete enquanto a condição for verdadeira: `while() {}`

:::


# Pacotes em R

Conjuntos de funções agrupadas juntas em um único "lugar"

## Exemplo: Tidyverse

Conjunto de pacotes que compartilham representações de dados, trabalhando harmonicamente entre si.

 * `ggplot2` (visualização de dados)
 * `dplyr` (manipulação de dados)
 * Outros: `tidyr`, `readr`, `purrr`, `tibble`, `stringr`, `forcats` e `lubridate`

Tidyverse torna simples a instalação de todos esses pacotes de uma vez.


# Gráficos em R

A biblioteca mais popular para gráficos em R é o `ggplot2`, incluída no Tidyverse [[ggplot2](https://ggplot2.tidyverse.org/)) e desenvolvida pelo mesmo grupo que criou o R Studio

## Por que gerar gŕaficos na linha de comando...


::: {.incremental}
::: {.fragment}
se eu posso fazer com ferramentas como o Excel?
:::

:::

## Alguns motivos

::: {.incremental}
 - A prática leva ao aumento da velocidade
 - Uso de código dá **maior controle** sobre os aspectos do gráfico
 - Com o código e os dados disponíveis, a visualização se torna **reprodutível**

:::

## Conceitos importantes sobre gráficos

[Raster vs. Vetor]{.eyebrow}

::: {.incremental}
 - Formatos de arquivo
   - Raster - cor e intensidade de pixels (`.jpeg`/`.jpg`, `.png`, `.tiff`, `.bmp`)
   - Vetor - formas (`.svg`, `.pdf`, `.eps`)
 - **Dê preferência para vetores**, evitando imagens "pixeladas"
 - **Exceções**: fotos, dados em grades, arquivo gigantes (c/ milhões/bilhões de pontos)

:::

## Conceitos importantes sobre gráficos

[Resolução vs. dimensão]{.eyebrow}

::: {.incremental}
 - Dimensão de pixel combina dois aspectos
   - Tamanho
   - Resolução (megapixel, dpi - "*dots per inch*")

:::

## Conceitos importantes sobre gráficos

[Acessibilidade]{.eyebrow}

::: {.incremental}
 - Vieses culturais (azul e vermelho, frio e calor)
 - Visualização por indivíduos com algum tipo de daltonismo
   - Maximizar diferenças em espectro e intensidade
 - Impressão em preto e branco

:::


## Alguns gráficos básicos

::: {.incremental}
 - Variáveis categóricas ou contínuas
 - Composição: `ggplot()`, geralmente incluindo a função `aes()` e o parâmetro `data=`
 - `geom_<name>` define o tipo de saída / gráfico

:::


## Para a próxima semana {.smaller}

- Relatório de aula prática
  - Aplicar os conceitos aprendidos em aula para análise exploratória de um novo conjunto de dados
  - Documentar o R Markdown com uma boa narrativa
  - Documentar os códigos
  - Incluir gráficos e tabelas com os resultados obtidos e interpretá-los
  - Recomendação de leitura para aspectos de reprodutibilidade: [Guia de Pesquisa Reprodutível The Turing Way](https://book.the-turing-way.org/reproducible-research/reproducible-research/)
  - Entrega para o professor até 25/08/2026


## Referências e recursos {.smaller}

- **R Crash Course for Biologists: An Introduction to R for bioinformatics and biostatistics (coding and Quantitative Biology)** by Robert I. Colautti (2022). ***[Versão gratuita no GitHub](https://github.com/ColauttiLab/RCrashCourse_Book)***.
- **Guide for Reproducible Research: The Turing Way**. ***[Versão online (em inglês)](https://book.the-turing-way.org/reproducible-research/reproducible-research/)***.
